# My Cypress Automation Project

This is a test automation project using Cypress.

## Prerequisites
- Node.js (version 18 or higher)

## Installation
1. Clone the repository
2. Run `npm install` to install dependencies

## Running Tests
- To open Cypress Test Runner: `npm run cy:open`
- To run tests in headless mode: `npm run cy:run`